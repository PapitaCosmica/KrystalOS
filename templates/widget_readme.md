# {{ name }} (KrystalOS Widget)

**Version:** {{ version }}  
**Author:** {{ author }}  
**Language:** {{ runtime.language }} ({{ runtime.version }})  

---

## 🚀 Overview

This is an auto-generated KrystalOS widget created with `krystal make:widget`. 
To see your widget in action, ensure it resides in the `widgets/` folder of your KrystalOS project and run:

```bash
krystal serve start
```

---

## 🛠️ Requirements

Since this widget runs on **{{ runtime.language }}**, ensure that your local environment has the execution binary installed and available in your system PATH, or provide a portable binary inside the KrystalOS `bin/` directory.

---

## 📡 Event Guide (The Nervous System)

KrystalOS widgets communicate via a high-performance real-time WebSocket Event Bus. Based on your `krystal.json` manifest, here are the events your widget interacts with:

### Events Emitted (What you broadcast to others)
{% if capabilities.events_emitted %}
{% for ev in capabilities.events_emitted %}
- `{{ ev }}`
{% endfor %}
{% else %}
*This widget does not emit any events.*
{% endif %}

### Events Subscribed (What you listen to)
{% if capabilities.events_subscribed %}
{% for ev in capabilities.events_subscribed %}
- `{{ ev }}`
{% endfor %}
{% else %}
*This widget does not listen to any events.*
{% endif %}

---

## 🎨 Grid & UI Specifications

- **Expected Grid Size:** {{ ui.grid_size.w }}x{{ ui.grid_size.h }}
- **Theme Color:** `{{ ui.theme_color }}`
- **Icon:** {{ ui.icon }}

Your interface is rendered inside a **Shadow DOM** for security. Do not attempt to use `document.querySelector` targeting elements outside your widget.

---
_Auto-generated by KrystalOS Phase 5 DX Agent._
